info:
  name: Delete Favorite
  type: http
  seq: 3

http:
  method: DELETE
  url: "{{baseUrl}}/favorites/:courseId"
  params:
    - name: courseId
      value: "1"
      type: path
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
