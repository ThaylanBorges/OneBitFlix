info:
  name: Favorites Courses
  type: http
  seq: 1

http:
  method: POST
  url: "{{baseUrl}}/favorites/:id"
  params:
    - name: id
      value: "2"
      type: path
  body:
    type: json
    data: ""
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
  forwardAuthorizationHeader: true
