info:
  name: All Categories
  type: http
  seq: 1

http:
  method: GET
  url: "{{baseUrl}}/categories?page=&perPage="
  params:
    - name: page
      value: ""
      type: query
    - name: perPage
      value: ""
      type: query
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

docs: This request retrieves a list of users from the JSONPlaceholder API.
