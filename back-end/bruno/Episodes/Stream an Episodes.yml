info:
  name: Stream an Episodes
  type: http
  seq: 1

http:
  method: GET
  url: "{{baseUrl}}/episodes/stream/:id"
  params:
    - name: id
      value: ""
      type: path
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
