info:
  name: Find Episode progess
  type: http
  seq: 3

http:
  method: GET
  url: "{{baseUrl}}/episodes/:id/watchTime"
  params:
    - name: id
      value: "2"
      type: path

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
