info:
  name: Delete Like
  type: http
  seq: 2

http:
  method: DELETE
  url: "{{baseUrl}}/likes/:id"
  params:
    - name: id
      value: ""
      type: path
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
