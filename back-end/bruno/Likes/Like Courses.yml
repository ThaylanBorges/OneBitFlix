info:
  name: Like Courses
  type: http
  seq: 1

http:
  method: POST
  url: "{{baseUrl}}/likes/:id"
  params:
    - name: id
      value: "1"
      type: path
  body:
    type: json
    data: ""
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
  forwardAuthorizationHeader: true
