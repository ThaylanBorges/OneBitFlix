info:
  name: "Update User "
  type: http
  seq: 4

http:
  method: PUT
  url: "{{baseUrl}}/users/current/update"
  body:
    type: json
    data: |-
      {
        "lastName": "Borges"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
