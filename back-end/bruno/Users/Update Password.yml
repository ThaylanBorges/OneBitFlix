info:
  name: Update Password
  type: http
  seq: 5

http:
  method: PUT
  url: "{{baseUrl}}/users/current/password"
  body:
    type: json
    data: |-
      {
        "currentPassword": "150963", 
        "newPassword": "123456"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
