info:
  name: Watching Episode for User
  type: http
  seq: 3

http:
  method: GET
  url: "{{baseUrl}}/users/current/watching"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
