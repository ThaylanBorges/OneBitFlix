info:
  name: Curent User
  type: http
  seq: 6

http:
  method: GET
  url: "{{baseUrl}}/users/current"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
