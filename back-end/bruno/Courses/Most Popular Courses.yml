info:
  name: Most Popular Courses
  type: http
  seq: 5

http:
  method: GET
  url: "{{baseUrl}}/courses/popular"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
