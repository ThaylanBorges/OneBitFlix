info:
  name: Newest Courses
  type: http
  seq: 3

http:
  method: GET
  url: "{{baseUrl}}/courses/newest"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
